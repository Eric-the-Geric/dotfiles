#!/usr/bin/env bash

set -euo pipefail

project_dir="${TERMINAL_GOL_DIR:-$HOME/Documents/personal_projects/terminal_gol}"
python_bin="$project_dir/.venv/bin/python"

if [[ ! -x "$python_bin" ]]; then
    python_bin="$(command -v python3)"
fi

exec "$python_bin" "$project_dir/main.py" "$@"
